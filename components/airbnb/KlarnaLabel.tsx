export default function KlarnaLabel() {
  return (
    <span className="flex items-center gap-1.5 text-xs text-[#8A7B6C]">
      <span
        className="inline-flex items-center justify-center w-4 h-4 rounded-full text-[9px] font-bold"
        style={{ backgroundColor: "#FFB3C7", color: "#1F1A16" }}
        aria-hidden="true"
      >
        K
      </span>
      Pay in 3 with Klarna — 0% interest
    </span>
  );
}
